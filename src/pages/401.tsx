import React from "react";

export default function Error401() {
  return (
    <div className="h-screen fixed flex flex-col items-center justify-center w-full font-bold">
      Not Authorized Page
    </div>
  );
}
